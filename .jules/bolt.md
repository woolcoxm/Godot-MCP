## 2026-05-04 - Array Iteration Optimization\n**Learning:** Multiple `.filter(x).length` calls for counting specific items in an array creates an O(k*n) iteration pattern (where k is the number of filters) and allocates intermediate arrays unnecessarily.\n**Action:** Use a single-pass `reduce` operation to aggregate multiple counters in one pass to achieve O(n) complexity and avoid creating temporary arrays.
